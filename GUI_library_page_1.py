import os           
from tkinter import *   
import datetime          

root = Tk()
root.title("Educators Library")

def see_last_readers():
    os.startfile("readers_names.txt")


def go_to_library():
    os.startfile("GUI_library_main_page.py")


def exit():
    root.destroy()

root.geometry("2000x2000")
root.minsize(200, 200)
root.title("Library")

Label(root, text="Welcome To Eductors Library", font=("Impact", 70), fg="green", bg="yellow", pady=30, padx=131).grid(row=0, column=0)


Button(root, text="Go To Library →", font=("Calibribody", 20), relief=None, bg="lightblue", pady=4, command=go_to_library).grid(row=50)

Button(root, text="Library Record History", font=("Calibribody", 20), relief=None, bg="lightblue", pady=4, command=see_last_readers).grid(row=51)

Button(root, text="Exit", font=("Calibribody", 20), fg="black", relief=None, bg="lightblue", command=exit).grid(row=52)


root.mainloop()












































































































































































































